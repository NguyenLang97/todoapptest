const INITIAL_STATE = {}

const todosReducers = (state: INITIAL_STATE, action) => {
    switch (action.type) {
        case:
        default: 
        return state    }
}

export default todosReducers